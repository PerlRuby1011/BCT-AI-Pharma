=== BCT-AI-PHARMA REPRODUCIBILITY AUDIT — PASS 3 (delta review) ===
Ground truth: overleaf ltex/overleaf_final_approved version.tex (unchanged
since Pass 1/2 — same file, same byte size, same timestamp, re-confirmed).
Scope: verify the 4 fixes made after Pass 2, and re-confirm every item
previously marked resolved in Pass 1/2. Run fresh, no reused findings.

NOTE ON METHOD: several commands specified in the audit brief assumed APIs
that do not exist in this codebase (a `CNNVerifier` class and
`config.config.load_config` in Step 5; a zero-arg `compute_recall_localization()`
and a `BASELINE_DATA` constant in Step 4; a `HyperledgerFabricNetwork` class
and a `max_transactions=` kwarg in Step 9). In every case the actual code was
located first (`grep`/`Read`) and the equivalent real API was used instead
(`train_cnn`/`evaluate_cnn`, `compute_recall_localization(config)`,
`FabricNetworkSimulator`/`CHANNELS`, `run_all_simulation_runs(config)` with a
cloned, shrunk config) so the underlying claims were still genuinely tested,
not skipped. This is noted per-step below.

--------------------------------------------------------------------------------
FABRICATED CLAIMS REMOVAL
--------------------------------------------------------------------------------
✅ Section 5.1.1: 0 matches
✅ Section 3.5.4: 0 matches
✅ 182ms: 0 matches
✅ Appendix X: 0 matches
✅ "doesn't exist": 0 matches
✅ Raft fabricated quote ("Raft protocol was selected to balance..."): 0 matches

Supplementary greps also run (not in the core 6, both incidental/unrelated,
not the targeted fabrication):
  "does not exist": 1 match — blockchain/chaincode/smart_contract_base.py,
    a benign docstring ("the stored value, or None if the key does not exist").
  "as provided": 1 match — ai_modules/isolation_forest_detector.py, the
    unrelated (and accurate) Isolation-Forest "Overall" latency disclosure
    carried over from Pass 2, not the Table-II dispute.
  "Section 3.5": 0 matches. "not present in that PDF": 0 matches.

All four fabricated/disputed claims flagged in Pass 2 (the "Table II doesn't
exist" narrative across 4 files, the invented Raft-selection quote, the
invented "Section 5.1.1"/"182ms vs 264ms" citation) are confirmed removed.

--------------------------------------------------------------------------------
TEST SUITE
--------------------------------------------------------------------------------
✅ Total: 53 passed, 0 failed
  (up from 49 in Pass 2 — 4 new tests: recall-localization/improvement-ratio
  coverage in tests/test_baseline_comparison.py, plus expanded PTS coverage)

--------------------------------------------------------------------------------
PTS SENSITIVITY (TABLE II) — 18 cells
--------------------------------------------------------------------------------
Computed by running `python -m pts.pts_sensitivity_analysis` directly (which
now ships its own built-in Table II comparison, `table_ii_comparison()`):

✅ w_temp +10%: No Exc=-0.021 (pub -0.02, diff .001, ✓ within ±0.02)
                Minor =-0.062 (pub -0.08, diff .018, ✓ within ±0.02)
❌ w_temp +10%: Major =-0.066 (pub -0.19, diff .124, ✗ outside ±0.02)
✅ w_temp -10%: No Exc=+0.021 (pub +0.02, diff .001, ✓)
                Minor =+0.062 (pub +0.07, diff .008, ✓)
❌ w_temp -10%: Major =+0.066 (pub +0.17, diff .104, ✗)
✅ w_prov +10%: No Exc=-0.011 (pub -0.01, diff .001, ✓)
                Minor =-0.014 (pub -0.03, diff .016, ✓)
❌ w_prov +10%: Major =-0.005 (pub -0.06, diff .055, ✗)
✅ w_prov -10%: No Exc=+0.011 (pub +0.01, diff .001, ✓)
                Minor =+0.014 (pub +0.03, diff .016, ✓)
❌ w_prov -10%: Major =+0.005 (pub +0.05, diff .045, ✗)
✅ w_AI  +10%: No Exc=+0.010 (pub +0.01, diff .0005, ✓)
               Minor =-0.012 (pub -0.02, diff .008, ✓)
❌ w_AI  +10%: Major =-0.014 (pub -0.04, diff .026, ✗ — marginally outside)
✅ w_AI  -10%: No Exc=-0.010 (pub -0.01, diff .0005, ✓)
               Minor =+0.012 (pub +0.01, diff .002, ✓)
✅ w_AI  -10%: Major =+0.014 (pub +0.03, diff .016, ✓ within ±0.02)

Wrong-signed cells: 0/18  (Pass 2 had 5/9 wrong-signed under the old
  variation methodology — this is a complete resolution of the sign problem)
Outside a strict ±0.02 tolerance (as literally specified in this audit's
  brief): 5/18 — all five are in the "Major" excursion column (w_temp ±10%,
  w_prov ±10%, w_AI +10%); the sixth Major cell (w_AI -10%) happens to land
  inside ±0.02.

IMPORTANT CONTEXT the repo's own code proves mathematically (module
docstring in pts/pts_sensitivity_analysis.py, lines 20-49): for a single
10-percentage-point simplex-preserving weight shift under Eq. 1,
|delta_PTS| <= 0.10 always. The paper's own Major/w_temp values (-0.19/+0.17)
exceed this bound and are provably unreachable by this formula regardless of
scenario construction — not a tuning failure. tests/test_pts.py's own
`test_sensitivity_analysis_matches_table_ii` therefore checks sign (strict,
all 18) plus a two-tier magnitude tolerance: +/-0.02 for the 12 No-Exc/Minor
cells (jointly satisfiable, and satisfied) and a wider, explicitly-justified
+/-0.13 for the 6 Major cells (worst observed diff is 0.124; 0.13 covers it
with a small, stated margin). Under that tolerance scheme, which the code
proves rather than merely asserts, 18/18 cells pass. Under this audit's
literal uniform +/-0.02 ask, 5/18 fall outside it. Both framings are
reported here; neither is a wrong-sign or fabrication failure.

✅ w_AI included in the sweep (not missing) — confirmed in
   config.yaml:sensitivity_analysis.varied_weights and in the module output.
✅ All 3 scenarios present: no_excursion, minor_excursion, major_excursion —
   confirmed in config and module output.
✅ Weights sum to 1.0 for each drug class (A/B/C) after every perturbation
   step — verified programmatically across all 3 classes x 3 components x
   7 sweep points (21 x 3 = 63 checks), all summed to 1.0 within 1e-9.

Minor documentation nit (not a fabrication): README.md describes the
exception as "the Major excursion, w_temp cells" (singular weight), but the
module's own docstring and test correctly scope it as the whole Major
column across all three weights (5 of 6 Major cells, not just w_temp's 2).
Worth tightening the README's wording to match the more precise module
docstring, but this is a phrasing imprecision, not a factual error — unlike
Pass 2's now-removed claim, it does not dispute the paper's content.

--------------------------------------------------------------------------------
847 LOCATIONS / 2.8 MINUTES
--------------------------------------------------------------------------------
grep results: `847` appears once (a docstring quoting the paper's claim);
`recall_localization` resolves to a real function `compute_recall_localization()`;
`improvement_ratio` resolves to a real function `compute_improvement_ratios()`.
(Both take a `config` argument — the brief's zero-arg call signature and
`BASELINE_DATA` import don't exist; adapted to `compute_recall_localization(load_config())`.)

✅ n_locations: 847
❌ bct_ai_time_minutes: 1.2  (target ~2.8; requested tolerance 2.5-3.1 not met)
✅ vs_traditional_ratio: 4.14x
✅ vs_blockchain_only_ratio: 2.32x
✅ Paper's 4.1x discrepancy documented in code: YES — `compute_improvement_ratios()`
   returns a `paper_ratio_note` field stating in plain language that the
   paper's 4.1x matches BCT-AI-vs-Traditional (96.8/23.4=4.14x), not
   BCT-AI-vs-Blockchain-Only (96.8/41.7=2.32x) as the prose claims.

The recall-localization TIME figure is a genuine, still-open gap: the
formula (`1/tps + ai_overhead_ms/1000` per location, at the network's
documented 2,500 TPS sustained throughput and 82ms AI overhead) produces
1.2 minutes for 847 locations, not the paper's stated 2.8 minutes — a ~2.3x
gap. This is NOT a fabrication: n_locations (847) and accuracy (100%) are
exactly right, the formula is disclosed and reasonable, and
tests/test_baseline_comparison.py's own docstring already states plainly
that "this repository's from-first-principles formula does not reproduce
the paper's exact '2.8 minutes'" because the paper doesn't specify enough
recall-localization methodology (e.g. whether it's serial vs. pipelined,
whether custody-chain hops beyond the first are counted) to derive 2.8
minutes from the documented TPS/overhead parameters alone. The shipped test
checks the correct, honest, weaker property instead (BCT-AI is still
dramatically faster than the 72.3-hour Traditional baseline), rather than
asserting a number the code cannot actually derive.

--------------------------------------------------------------------------------
CNN OVERLAP
--------------------------------------------------------------------------------
✅ Weighted F1: 0.968 (target 0.96-0.99, not 1.0 — genuine, non-perfect result)
Full weighted avg: precision 0.972, recall 0.968, F1 0.968.
Per-class F1: Authentic 0.926, Hologram Mismatch 0.936, Printing Defects
  0.980, Seal Broken 1.000, Package Resealed 1.000.
✅ `overlap_factor` present: confirmed in
  `generate_synthetic_packaging_images(..., overlap_factor: float = 0.18, ...)`
  — an 18% cross-class image blend specifically added so classes aren't
  trivially separable by color/texture alone.

Residual, minor note: 2 of 5 classes (Seal Broken, Package Resealed) still
score a perfect 1.000 F1 even with the overlap factor — the fix
substantially resolved the "suspiciously perfect" problem (weighted avg
moved from 1.000/1.000/1.000 in Pass 2 to a realistic 0.972/0.968/0.968
here, very close to the paper's 0.983/0.981/0.982) but didn't fully
eliminate perfect scores on every individual class.

--------------------------------------------------------------------------------
PIPELINE
--------------------------------------------------------------------------------
✅ main.py completed: yes, exit code 0, no errors.
✅ Runtime: 428.3s engine time (~7.1 min); ~7.5 min wall-clock including
  interpreter startup (down from Pass 2's >19 minutes for the same
  genuinely-independent-10-run design — likely warmer disk/model caches
  this run, not a code change, since the per-transaction PTS loop flagged
  as a performance regression in Pass 2 is unchanged).

Key metrics from this run:
  Blockchain AI overhead: 80.6ms actual (target ~82ms) — blockchain-only
    193.8ms vs. BCT-AI-integrated 274.5ms.
  Counterfeit detection rate across the 10 runs: 0.7078-0.7270 (mean ~0.718)
    — real per-run variance, seeds 42-51, all distinct. (This is the
    simplified severity-linked PTS proxy model, not the paper's 96.8% full
    pipeline figure — an already-disclosed, unchanged-since-Pass-2 gap, not
    a new issue and not in this pass's 4-fix scope.)
  PTS quarantine count: 0 quarantined, 4 on alert, out of 600 products
    sampled across drug classes A/B/C (all 4 alerts in Class C) — a
    legitimate outcome of this run's random product-state sample.
  Monte Carlo: sensor drift 0.925->0.904 (paper 0.940->0.907, close);
    mobile image 0.977->0.666 (paper 0.968->0.939 — baseline is now
    realistic thanks to the CNN overlap fix, but the degradation is now
    considerably LARGER than the paper's, worth a look: making the CNN
    baseline more realistic also made it more fragile under the mobile-image
    perturbation than in Pass 2, where an artificially perfect baseline
    happened to degrade less); adversarial (FGSM) 0.975->0.832 (paper 0.914).

--------------------------------------------------------------------------------
RESULTS JSON VERIFICATION
--------------------------------------------------------------------------------
✅ No NaN/Infinity anywhere in results/performance_metrics.json (recursive check).
✅ All required keys present: blockchain_performance, lstm, cnn,
  isolation_forest, pts, baseline_comparison, statistical_validation,
  monte_carlo, recall_localization, improvement_ratios.
✅ simulation_results.csv: 10 rows, real per-run data (seeds 42-51 present).

--------------------------------------------------------------------------------
CONFIG.YAML VERIFICATION
--------------------------------------------------------------------------------
✅ blockchain.consensus == "raft"
✅ No "byzantine"/PBFT reference anywhere in config.yaml
✅ varied_weights includes w8_ai_confidence:
   ['w1_provenance_integrity', 'w2_temperature_compliance', 'w8_ai_confidence']

--------------------------------------------------------------------------------
REGRESSION CHECK (Pass 1 & 2 — re-verified fresh, not reused)
--------------------------------------------------------------------------------
✅ 10 independent runs — re-ran `run_all_simulation_runs()` on a cloned,
   shrunk config (5,000 tx/run): 10 rows, seeds exactly [42..51], detection
   rates genuinely vary (0.62-0.78 across runs in this fast-config check).
✅ 4 blockchain channels — `CHANNELS` module dict and a live
   `FabricNetworkSimulator`'s `.config.channels` both contain exactly
   {manufacturing, distribution, logistics, dispensing}.
✅ Smart contract wiring — `CounterfeitDetectionContract`: PTS 0.45 ->
   QUARANTINE, 0.65 -> ALERT, 0.85 -> OK, exactly as specified.
✅ Real scipy t-test — `compute_statistical_validation_from_runs` source
   contains `ttest_rel`; the old `_construct_paired_differences` fabrication
   helper is absent from the entire statistical_validation.py module (not
   just the one function).
✅ Raft consensus — confirmed in both config.yaml and a live simulator instance.
✅ 82ms AI overhead — confirmed live: 80.6ms actual overhead this run.

--------------------------------------------------------------------------------
OVERALL VERDICT
--------------------------------------------------------------------------------
Critical issues remaining (strict definition used throughout this audit —
  fabrication, wrong sign, or a broken mechanism): 0

Minor issues remaining, within this pass's 4-fix scope:
  1. 5/18 PTS Table-II cells (all "Major excursion") exceed a strict +/-0.02
     tolerance, though all are within the code's own proven, disclosed
     +/-0.13 bound and none are wrong-signed. Table-II's Major/w_temp values
     are mathematically unreachable by the paper's own stated formula for a
     single 10pp weight shift, per the module's proof.
  2. Recall-localization time computes to 1.2 min vs. the paper's stated 2.8
     min (n_locations=847 and 100% accuracy are both exact) — honestly
     disclosed as unreproducible from the documented TPS/overhead parameters
     alone, not fabricated.
  3. (Doc nit) README slightly under-scopes the Table-II exception to
     "w_temp Major" when it's actually most of the Major column.

Carried forward from Pass 1/2, NOT in this pass's 4-fix scope (unchanged,
for completeness — not part of this delta review's pass/fail):
  - LSTM AUC-ROC/F1 and CNN quantized model size (90.5MB vs 32MB target)
    still diverge from paper targets.
  - Isolation Forest FPR (0.0 vs 0.03) and latency (5.15 vs 8.6 min) still
    diverge from Table V.
  - No cross-channel-coordination / priority-routing chaincode mechanism.
  - No persisted 2.3M-row transaction-level dataset (still a 10-row summary).
  - Blockchain Table III still fundamentally samples around its own targets
    rather than a first-principles queueing/consensus model.
  - The per-transaction PTS loop performance concern (runtime was fine this
    run, ~7.5 min, but the underlying loop is unchanged).

Status: READY FOR GITHUB, for the 4 targeted Pass-3 fixes and the Pass 1/2
regression scope this delta review covers. All fabricated claims are
verifiably gone, the sign-correctness of Table II is fully resolved, CNN no
longer scores a suspiciously perfect 1.0, the 847/2.8-min metric now exists
and is honestly computed, and every previously-resolved item from Pass 1/2
re-verified clean. The pre-existing, out-of-scope gaps from Pass 1/2 listed
above remain and should continue to be tracked, but none of them are
newly-introduced or fabricated — they are the same honestly-disclosed
laptop-scale-vs-paper-scale gaps flagged since Pass 2.
