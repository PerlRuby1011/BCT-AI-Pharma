# Runtime dependencies, pinned to exact versions verified to install and
# run together (Python 3.9-3.11, CPU-only). For development-only tools
# (test runner), see requirements-dev.txt.
#
# Known transitive conflict: tensorflow 2.16.2 declares numpy<2.0, while
# grad-cam's opencv-python dependency declares numpy>=2. numpy==1.26.4
# (pinned below) satisfies tensorflow and has been verified compatible with
# the installed opencv-python build at runtime (pip's resolver will still
# print a conflict warning; it is safe to ignore).
tensorflow==2.16.2
torch==2.2.2
torchvision==0.17.2
scikit-learn==1.6.1
numpy==1.26.4
pandas==2.3.3
matplotlib>=3.7
seaborn>=0.12
jupyter>=1.0
grad-cam==1.5.5
pyyaml>=6.0
scipy>=1.10
